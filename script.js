let dog = document.getElementById("dog"); let scoreText = document.getElementById("score"); let message = document.getElementById("message"); let ending = document.getElementById("ending"); let score = 0; const messages = [ "🐶 Sorry!", "🦴 Here's a bandage!", "❤️ I didn't mean to bite!", "😊 You're doing great!", "💕 Get well soon!" ]; moveDog(); dog.onclick = function(){ score++; scoreText.innerText = score; message.innerText = messages[Math.floor(Math.random()*messages.length)]; if(score >= 15){ dog.style.display = "none"; ending.style.display = "block"; return; } moveDog(); } function moveDog(){ let x = Math.random() * (window.innerWidth - 100); let y = Math.random() * (window.innerHeight - 150); dog.style.left = x + "px"; dog.style.top = y + "px"; }
